const appColors = require('../appColors')

const styles = {
  container: {
    textAlign: 'center'
  },
  avatar: {
    width: 80,
    height: 80,
    margin: '0 auto 10px',
    backgroundColor: appColors.primary.main
  },
  icon: {
    fontSize: 50
  }
}
module.exports = styles
